{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "cppbuild",
            "command": "g++",
            "args": [
                "${fileBasename}",
				"-o",
				"${fileBasenameNoExtension}.bin"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": {
                "base": "$gcc",
                "fileLocation": [
                    "relative",
                    "${fileDirname}"
                ]
            },
            "group": "build",
            "detail": "Attività generata dal debugger."
        }
    ],
    "version": "2.0.0"
}