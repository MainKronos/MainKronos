{
    "tasks": [
        {
            "type": "shell",
            "label": "cppbuild",
            "command": "make",
            "args": [
                "${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": {
                "base": "$gcc",
                "fileLocation": [
                    "relative",
                    "${fileDirname}"
                ]
            },
            "group": "build",
            "detail": "Attività generata dal debugger."
        }
    ],
    "version": "2.0.0"
}